interface AdSenseSlotProps {
  variant: 'banner' | 'sidebar' | 'in-content' | 'mobile' | 'social';
  className?: string;
}

export function AdSenseSlot({ variant, className = '' }: AdSenseSlotProps) {
  // All Adsterra code completely removed
  // This component now returns nothing - safe for AdSense approval
  return null;
}